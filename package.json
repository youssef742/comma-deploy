{
  "name": "comma-final",
  "version": "1.0.0",
  "description": "Combined frontend and backend for Comma project",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "cd comma-dashboard && npm install && npm run build",
    "serve-frontend": "serve comma-dashboard/dist"
  },
  "dependencies": {
    "@iconify/vue": "^4.3.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.6.0",
    "serve": "^14.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
